- 采用 Monorepo 架构，`shared_lib` 作为核心依赖被 `server` 和 `desktop_app` 引用，确保隧道协议、加密算法及身份认证逻辑的一致性。
- `server` 提供控制平面 API 与数据中继服务，`desktop_app` 作为边缘节点发起连接请求并执行 NAT 穿透，两者通过共享的通信协议进行交互。
- 根目录配置了统一的 CI/CD 工作流（`.github/workflows`），分别针对服务端构建、客户端打包及共享库测试进行自动化管理。