- 采用 Monorepo 结构，通过 `shared_lib` 模块统一封装隧道协议、加密算法及身份认证逻辑，确保服务端与客户端的行为一致性。
- 服务端 (`server`) 负责控制平面信令交互、节点注册及数据中继，作为网络拓扑的中心枢纽。
- 桌面客户端 (`desktop_app`) 基于 Wails 框架实现跨平台 GUI，通过调用共享库能力发起 P2P 连接或 fallback 至服务端中继。
- 各组件间通过标准化的 API 接口与共享数据结构进行通信，解耦业务逻辑与底层网络实现。