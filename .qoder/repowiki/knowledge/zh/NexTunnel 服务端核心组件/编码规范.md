- 所有 HTTP 处理器统一使用 `writeJSON` 或 `writeAPIError` 封装响应格式，确保 API 返回结构一致性。
- 关键业务模块（如 controlplane, dashboard）采用接口驱动设计，通过 `Store` 接口屏蔽底层 SQLite 或内存实现细节。
- 服务启动时优先从配置文件或 CLI 参数加载配置，并内置默认值（如 `DefaultConfig`），同时支持通过 `-tls-*` 参数动态开启 mTLS。