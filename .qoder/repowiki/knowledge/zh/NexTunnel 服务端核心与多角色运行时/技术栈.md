- QUIC 数据面：`quic-go/quic-go v0.59.1` 用于 `internal/relay` 的 `QUICTransport`。
- eBPF/XDP 加速：`cilium/ebpf v0.11.0` 配合 `internal/ebpf/xdp_forwarder.c` 实现内核态转发。
- NAT 探测：`pion/stun/v2` 与 `pion/turn/v4` 支撑 `internal/natdetect` 的 STUN/TURN 能力。
- 持久化：`modernc.org/sqlite` 为控制面与 Dashboard 提供 SQLite 存储（可选内存模式）。
- GeoIP：`oschwald/maxminddb-golang` 用于 `internal/anycast` 的地理定位。