- 所有协议消息结构体均遵循 `*Message` 命名后缀，并配套提供 `New*Message` 工厂函数以统一处理 JSON 序列化逻辑。
- 涉及并发访问的资源管理器（如 `ipam.IPAM`、`protocol.Conn`、`audit.JSONFileAuditLogger`）均在内部集成 `sync.Mutex` 或 `sync.RWMutex` 以保证线程安全。
- 工具类包（如 `crypto`、`tlsutil`）倾向于暴露无状态的纯函数接口，而状态管理组件（如 `ipam`、`audit`）则通过构造函数返回具体的结构体实例。