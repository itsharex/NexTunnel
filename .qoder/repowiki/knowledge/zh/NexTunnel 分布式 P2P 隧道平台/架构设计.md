- 采用 Go Workspaces (`go.work`) 将 `server`、`desktop` 和 `pkg` (shared_lib) 聚合为单一逻辑仓库，支持跨模块本地开发与依赖解析。
- 根目录维护统一的 CI/CD 流水线（`.github/workflows`），协调服务端部署、客户端构建及协议库测试的自动化流程。
- 通过 `deploy/server` 提供标准化的容器化部署方案（Docker Compose），实现服务端环境的快速交付与运维。