- 采用 Go Workspaces (`go.work`) 将 `server`、`desktop` 和 `pkg` (shared_lib) 聚合为单一开发上下文，支持本地跨模块即时编译与调试。
- 遵循分层架构设计：`pkg` 提供无状态的协议编解码与加密原语；`server` 实现控制面信令处理与数据面中继逻辑；`desktop` 作为边缘节点集成 P2P 引擎并调用共享库进行安全通信。
- 通过 `.github/workflows` 中的 CI/CD 流水线协调各子模块的构建、测试与发布，确保多角色运行时（中继、边缘、控制）的版本一致性与交付质量。