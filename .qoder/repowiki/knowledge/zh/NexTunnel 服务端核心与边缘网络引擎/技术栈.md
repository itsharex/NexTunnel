- **传输层**：使用 `quic-go/v0.59.1` 实现 QUIC 协议支持，结合原生 TCP 监听器。
- **前端技术**：集成 Vue 3 + TypeScript + Vite 构建的管理控制台 (`web/`)。
- **底层优化**：利用 eBPF (XDP) 进行高性能数据包过滤与转发 (`internal/ebpf`)。
- **网络工具**：集成 `pion/turn` 和 `pion/stun` 用于 NAT 穿透检测。