- 所有带负载的消息类型均遵循工厂方法模式（如 `NewAuthMessage`），在内部统一调用 `marshalMessage` 进行 JSON 序列化并封装为 `*Message`。
- 协议消息负载统一采用 JSON 格式进行序列化与反序列化，结构体字段通过 `json` tag 明确标识。
- 网络连接的读写操作通过独立的 `sync.Mutex`（`readMu` 和 `writeMu`）实现细粒度并发控制，确保多线程环境下的帧完整性。