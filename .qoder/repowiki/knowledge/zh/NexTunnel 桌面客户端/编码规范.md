- 后端 API 方法（如 `GetTunnels`, `StartTunnel`）统一返回 `(result, error)` 格式，错误直接透传至前端处理。
- 前端 Store 中的异步操作（如 `startTunnel`）统一使用 `try-catch` 捕获错误并更新 `lastError` 状态，确保 UI 能反馈异常。
- 内部模块（如 `tunnel`, `p2p`, `config`）均包含 `doc.go` 文件，用于包级别的文档说明。
- 配置存储层 `Store` 采用 CRUD 模式封装 SQL 操作，对上层屏蔽具体的数据库查询细节。