- 后端导出的 Wails 绑定方法（如 `GetTunnels`, `StartTunnel`）均定义在 `App` 结构体上，并返回明确的错误类型或数据结构。
- 内部模块（如 `tunnel`, `p2p`, `quic`）普遍采用 `Config` 结构体配合 `New...` 构造函数进行依赖注入和初始化。
- 网络引擎组件（如 `Engine`, `QUICTransport`）使用 `atomic.Value` 或 `sync.Mutex` 管理并发状态，并通过 `context.Context` 控制生命周期。